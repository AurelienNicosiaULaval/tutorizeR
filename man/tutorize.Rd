% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_to_tutorial.R
\name{tutorize}
\alias{tutorize}
\title{Convert a source document into an interactive tutorial}
\usage{
tutorize(
  input,
  output_dir = NULL,
  format = c("learnr", "quarto-live"),
  assessment = c("code", "mcq", "both"),
  overwrite = FALSE,
  language = c("en", "fr"),
  seed = NULL,
  verbose = TRUE,
  output_file = NULL,
  question_bank = NULL,
  mcq_source = c("inline", "bank", "mixed"),
  lint_strict = FALSE
)
}
\arguments{
\item{input}{Path to source `.Rmd` or `.qmd` file.}

\item{output_dir}{Optional output directory. Defaults to source directory.}

\item{format}{Output format: `"learnr"` (default) or `"quarto-live"`.}

\item{assessment}{Assessment mode: `"code"`, `"mcq"`, or `"both"`.}

\item{overwrite}{Logical; overwrite existing output file?}

\item{language}{Message/template language: `"en"` (default) or `"fr"`.}

\item{seed}{Optional integer seed injected into setup chunk.}

\item{verbose}{Logical; show progress and diagnostic messages?}

\item{output_file}{Optional explicit output file path. Useful for wrappers.}

\item{question_bank}{Optional path(s) or `tutorize_question_bank` object.}

\item{mcq_source}{MCQ generation source: `"inline"`, `"bank"`, `"mixed"`.}

\item{lint_strict}{Logical; fail conversion when lint reports errors.}
}
\value{
A `tutorize_report` object (invisibly).
}
\description{
`tutorize()` is the canonical high-level API. It converts an existing
`.Rmd` or `.qmd` source document into a tutorial-ready document for
`learnr` or `quarto-live`.
}
\examples{
\dontrun{
rep <- tutorize("analysis.qmd", format = "learnr", assessment = "both")
print(rep)
}
}
